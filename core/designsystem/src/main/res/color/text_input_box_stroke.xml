<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:alpha="0.5" android:color="@color/background_registration_option" android:state_enabled="false"/>

    <!-- Focused -->
    <item android:color="@color/background_registration_option" android:state_focused="true"/>

    <!-- Hovered -->
    <item android:color="@color/background_registration_option" android:state_hovered="true"/>

    <!-- Normal -->
    <item android:color="@color/background_registration_option"/>
</selector>